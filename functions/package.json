{
  "name": "functions",
  "version": "1.0.1",
  "description": "Cloud Functions for GameVault SSR",
  "main": "lib/index.js",
  "scripts": {
    "copy-server": "mkdir -p dist/GameVault/server && if [ -d ../../dist/GameVault/server ]; then cp -r ../../dist/GameVault/server/* dist/GameVault/server/; else echo 'Warning: Server files not found at ../../dist/GameVault/server'; fi",
    "build": "npm run copy-server && tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-functions": "^6.0.0",
    "firebase-admin": "^13.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.17.19",
    "typescript": "~5.9.2"
  },
  "private": true
}

